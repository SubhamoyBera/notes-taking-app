import { styled } from '@mui/system';

export const useStyles = styled({
  root: {
    marginTop: '1em'
  },
  infoText: {
    marginBottom: '.5em',
    fontWeight: 500,
    color: '#00000099',
    fontSize: '1.2em'
  }
})